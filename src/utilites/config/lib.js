import { AsyncStorage } from 'react-native';
import axios from 'axios';
// import { Buffer } from 'buffer';

class Api {

  // static postAxios(route, formData, config) {
  //   return this.post(route, formData, config)
  // }
  // static getAxios(route) {
  //   return this.get(route)
  // }

  // static post = async (route, formData, config) => {    
  //   const base_url = 'http://g7technologies.com/mobile_gigs/api';
  //   const url = `${base_url}/${route}`;
  //   return axios.post(url,formData);
  // };


  // static get = async (route) => {
  //   const base_url = 'http://g7technologies.com/mobile_gigs/api';
  //   const url = `${base_url}/${route}`;
  //   //console.log(url);
  //   return axios.get(url);
  // }
}

export default Api;